/**
 * Enum for sound types.
 * @readonly
 * @enum {number}
 */
const SoundType = {
  /** @member {number} */
  BGM: 2048,
  /** @member {number} */
  SE: 4096,
  /** @member {number} */
  UNKNOWN: -1
};

Object.freeze(SoundType);
